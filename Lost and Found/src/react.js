/*
  Runtime placeholder for `react.d.ts`.
  This file is a no-op at runtime and exists so editors can resolve
  imports that referenced the previous `src/react.d.ts` shim.
*/

/* eslint-disable */

// Keep the module present but export nothing — safe for runtime.
export {};
